web-spinner-input
    web-input.queue(name="webInput")#webInput
    web-slide(mode="absolute",closeto="{{closeTo}}",opened="false",apperance="left",ease="Power3",name="slide",outdistance="620")
        #spinnerData
            #less
            #info
                #amount VALUE
                #text DAYS
            #more
        web-spinner(name="spinner")