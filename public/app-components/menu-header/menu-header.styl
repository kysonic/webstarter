@import "./public/stylesheets/vars";
@import "./public/stylesheets/mixins";
menu-header
  overflow hidden;
  .row#header
    height headerHeight;
    background secondaryColor;
    width 100%;
    display block;
    padding 19px 0;
    max-width 320px;
    overflow hidden;
    basicLightFontStyle(25px);
    #title-wrapper
      width 90%;
      display block;
      margin 0 auto;
      overflow hidden;
      #title
        padding 0
        text-align center;
        overflow hidden;
        white-space nowrap;
        font-family lightFamily;



